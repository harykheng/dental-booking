# Front end (Vite) environment variables — copy this file to `.env` and fill
# in real values from your Supabase project (Project Settings -> API).
# NEVER commit the real .env file.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
